# MOSAIBEADS 3.0：原理与调参指南

## 为什么不是简单缩小图片

普通平均池化把一个区域的所有像素求平均。它在平坦区域很稳定，但边界两边的颜色
会被混在一起：黑色轮廓和白色背景可能变成灰色，眼睛、胡须、天线等面积很小的
特征也会直接消失。拼豆真正需要的是“在有限格子里分配表达能力”：重要特征占一格，
不重要的纹理可以合并。

## 实际处理管线

### 1. 预处理与场景识别

程序修正照片方向和透明通道，把过大的原图缩到适合计算的尺寸。自动模式会检测
人脸，并结合边缘密度判断图片更接近人像、插画还是自然照片。随后使用双边滤波降低
噪声；人像还会轻度增强局部明暗，使五官在缩小前更容易被识别。

### 2. 建立“重要性地图”

程序综合四类信息：

- Sobel 边缘：轮廓和颜色断层；
- 局部对比：眼睛、嘴、纽扣等小结构；
- 频谱残差显著性：画面中最吸引注意的区域；
- 人脸保护区域：检测到的人脸获得额外权重。

它们合成一张显著性图，决定每一颗豆子更应该代表局部平均颜色，还是保留一个醒目
的小特征。

### 3. 结构感知降采样

每个目标豆格先计算稳定的面积采样颜色，再从原图对应区域的 Lab 颜色中提取中心加权、
显著性加权的主色和真实像素 medoid。平坦区域更接近稳定采样；轮廓和显著区域更多地
采用结构代表色，因此不会把边界全部平均成灰色。

如果一条线或一只眼睛只占格子很小面积，程序还会检查“高对比、连贯的少数颜色”。
它通过显著性门控后可被提升为该格代表色，这就是细线救援机制。纹理噪声没有足够的
结构证据，通常不会被提升。

### 4. 从完整品牌色板选择有限颜色

程序不是先粗暴取使用次数最多的若干颜色，而是进行加权的贪心设施选址：每加入一种
真实豆色，都计算它能为整幅图减少多少感知色差；显著格子的权重更高。这样有限的颜色
名额会优先覆盖主体，同时仍照顾少量但关键的颜色。

### 5. CIEDE2000 颜色匹配

RGB 欧氏距离不符合人眼感知，例如相同数值距离在深色和浅色区域看起来并不相等。
程序将颜色转换到 CIE Lab，并用 CIEDE2000 色差选择最接近的真实品牌豆色。

### 6. 边缘感知杂色清理

初次匹配后，程序用迭代条件模式（ICM）优化相邻格子：平坦区域里，孤立颜色会被周围
色块吸收；真实边缘和显著区域附近允许颜色跳变。这样清理杂点时不会统一抹掉轮廓。

### 7. 调色板、编辑和导出

自动结果是起稿。右侧上方把结果中全部真实豆色绘制成可点击色块；橙色边框、当前色块
和色号文字共同表示画笔颜色。点选颜色后，可以单击图纸格子或按住左键连续拖动填色。
下方仍保留完整色号、名称和用量表，并随编辑实时更新。

V3 把“当前选色”与“数量列表刷新”分离：重建表格时恢复原来的选中索引，因此填色、
撤销、重做或数量变化都不会偷偷切回第一个色号。色号表本身仍可点击，选择会同步到
上方色块。导出包包含豆孔预览、纯像素图、色号网格、PDF、用量 CSV 和 JSON 工程数据。

### 8. 智能调参如何工作

全参数智能调参把当前横向豆数和最多颜色当作搜索中心，而不是硬限制。它联合搜索全部品牌色板、
约 72%–128% 的横向豆数、约 60%–122% 的颜色数量、自动/人像/插画/照片四种类型、白/黑/透明留白
背景，以及特征保护、杂色清理、饱和度、对比度和抖动。程序用空间填充设计生成至少 16 套组合，
在保持相对尺寸差异的小预览上快速计算：

- SSIM：整体明暗与局部结构是否接近；
- 边缘 F1：轮廓、五官和线稿是否仍在；
- Lab / CIEDE2000：颜色在人眼感知上是否接近；
- 制作分：惩罚单颗孤立豆、过多碎片和过高颜色负担；
- 可选 AI 分：MobileNetV3 特征余弦相似度，判断主体的高层视觉特征。

制作分还会计入总豆格与颜色预算，所以较小、颜色较少的候选确实更容易进入“最易制作”。
程序不把这些目标粗暴合成唯一答案，而是先找“传神度和制作性都没有被另一方案同时超过”的
Pareto 前沿，再选出最传神、综合平衡和最易制作三种意图。最后按每套方案各自的真实品牌、豆数、
颜色预算和参数重新精算三张图。

### 参数锁定

品牌、背景、横向豆数、最多颜色、图片类型、特征保护、杂色清理、饱和度、对比度和抖动各自
拥有独立的“固定”开关。候选生成器在构造参数前读取锁定集合：锁定项直接复制当前值，只有未锁定
项才从搜索设计中取值。重复候选会被去重；锁定信息也会写入结果元数据，便于验证和复现。

若仅放开一个参数，三套方案可能因候选较少而部分相同，这是严格遵守锁定条件的正常结果；程序
不会为了强行制造三张不同图而偷偷改变已固定参数。

### 9. AI 模型的作用与边界

MobileNetV3-Small 在 ImageNet 上预训练，去掉分类头后输出 576 维视觉特征。原图与每张候选图
使用相同标准化预处理，特征归一化后计算余弦相似度。它能帮助识别“像素减少后主体是否还是那个
主体”，但不懂你手里的豆色库存，也不能代替色差和边缘评分，所以只占传神评分的 7%。

没有模型时，AI 分取中性值，传统指标继续工作。模型存在但 CUDA 不可用时会自动改用 CPU。

### 10. 指定豆色为什么能严格限制

普通模式先从整套品牌颜色里自动挑选子集；固定色板模式则跳过这一步，把用户确认的颜色数组直接
作为量化候选。后续 CIEDE2000 映射和杂色清理只能在这个数组的索引之间选择，因此输出像素在算法
层面不可能引用未选颜色，而不只是界面上的数量限制。

## 参数意义与调整方法

### 品牌色板

决定最后能使用哪些实物颜色和色号。手里是什么豆就选什么品牌。不同品牌的 RGB 数据
只是近似值，重要作品应使用实体、熨烫后的色卡最终校准。

### 横向豆数

决定分辨率和成品大小，纵向豆数按原图比例自动计算。5mm 豆下，横向 48 格约 24cm；
29 格是一块常见大板。

- 越小：豆少、风格概括，但更容易丢五官；
- 越大：更像原图，但成本、板数和制作时间增加；
- 单人头像建议 40–58，多人照片建议 70 以上，Logo 可从 24–40 开始。

### 最多颜色

限制图纸的颜色种类，不是强制一定使用这么多。

- 8–16 色：图形感强，适合 Logo 和简洁动漫；
- 16–28 色：头像、宠物常用范围；
- 24–36 色：风景和复杂照片；
- 太少会合并肤色/阴影，太多会产生采购压力和零碎小色块。

### 图片类型

- 自动：先尝试人脸检测，再按边缘和颜色复杂度选择；
- 人像/宠物：加强局部明暗和主体特征，侧脸或未被自动识别的脸可手动选它；
- 插画/动漫：更重视硬边、线稿和纯色块；
- 照片：处理更温和，保留连续色调。

### 特征保护

控制显著性、主色 medoid 和细线救援对结果的影响。

- 提高：眼睛、嘴、轮廓和细线更醒目；
- 降低：结果更接近稳定的区域采样，噪声更少；
- 人像/宠物建议 0.72–0.90，插画 0.65–0.85，风景 0.50–0.72；
- 拉到 1.0 并不总是更好：高 ISO 照片、毛发和树叶可能被误当成重要细节。

### 杂色清理

控制平坦区域相邻格子合并的力度。

- 提高：色块更干净，孤立豆更少；
- 降低：保留更多局部变化；
- Logo/动漫可用 0.68–0.90，人像通常 0.50–0.70，风景 0.35–0.58；
- 如果眼睛、嘴或文字笔画被吃掉，先降低它；如果背景出现许多单颗杂色，则提高它。

### 饱和度

在像素化前调整颜色鲜艳程度。实物豆和屏幕发光色不同，轻微提高到 1.03–1.12 常能
避免成品显灰；肤色过红、色块太艳时降到 0.90–1.00。

### 对比度

扩大或压缩明暗差。主体和背景黏在一起时可提高到 1.05–1.15；黑色区域堵成一片、
高光丢失时应降低。对比度过高会把阴影压黑，不等同于“更传神”。

### 抖动

用相邻两种豆色的排列模拟中间色。误差传播在重要边缘处会自动减弱。

- 关闭：默认推荐，适合人脸、文字、Logo 和动漫；
- 轻微：天空、渐变背景和风景；
- 明显：追求复古网点效果；会增加杂色感和逐颗制作难度。

### 显示豆孔 / 显示网格

只影响编辑预览，不改变颜色匹配。最终导出的带色号图纸始终具有可制作的网格和板界线。

## 快速排错

- **脸不像**：增加横向豆数 → 选择人像模式 → 特征保护略提高 → 杂色清理略降低。
- **颜色太碎**：降低最多颜色 → 提高杂色清理 → 关闭抖动。
- **背景抢主体**：先裁图，让主体占更大面积；这通常比把所有滑块拉满更有效。
- **轮廓断了**：提高特征保护、降低杂色清理，或增加 6–10 格宽度。
- **颜色显灰**：饱和度提高约 0.05–0.12，但仍应以实体色卡为准。
