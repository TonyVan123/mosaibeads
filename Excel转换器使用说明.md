# MOSAIBeads Excel 双向编辑使用说明

## 为什么 Excel 颜色和拼豆色号不是一回事

Excel 单元格的背景色保存的是屏幕显示用的 RGB/ARGB；拼豆色号则是品牌自己的库存编号，
两者没有天然的一一对应关系。MOSAIBeads 在可见单元格里同时写入色号文字和近似背景色，
并在隐藏工作表中保存品牌、完整色板以及原始格子数据，因此能无损识别自己导出的工作簿。

如果在 Excel 中换成一个并非品牌标准色的任意颜色，转换器会用 CIEDE2000 感知色差，
在当前品牌完整色板里寻找视觉上最接近的实体豆色。最终输出一定使用真实品牌色号。

## 从拼豆图纸导出 Excel

在 MOSAIBeads 主程序或手工编辑器中点击“导出图纸”，输出目录会包含
`*_editable.xlsx`。其中：

- “拼豆图纸”页用于手工修改；
- “完整色板”页列出当前品牌全部可选色号；
- 隐藏的 `MOSAIBeads_Data` 页是往返转换所需的数据，请不要删除或改名。

## 在 Excel 中修改

可以选择单格、矩形区域、整行或整列，然后修改背景填充色。也可以直接修改单元格里的
色号文字。规则如下：

- 只改背景色：按新背景色匹配最近的品牌豆色；
- 只改色号文字：使用该品牌中对应的色号；
- 背景色和文字同时修改且不一致：以背景色为准，并在转换报告中计为冲突；
- 未修改的单元格：保持原色号，不会因显示 RGB 的微小差异而漂移。

推荐从“完整色板”页复制所需色号或使用取色器；任意 Excel 主题色也能转换，但可能只能
近似到品牌现有颜色。

## 转换回拼豆图纸

1. 运行 `MOSAIBeads_Excel_Converter.exe`。
2. 点击“选择 Excel”，打开 MOSAIBeads 导出的 `*_editable.xlsx`。
3. 查看预览和修改统计；近似颜色数较多时建议检查关键细节。
4. 点击“导出完整图纸”，得到 PNG、PDF、用量表、JSON 和新的可编辑 Excel。

## 手工编辑器的批量操作

新版手工编辑器提供“矩形选择”“整行”“整列”和“填充选区”。完整品牌色板可搜索，
右键格子仍是吸取该格颜色。还支持 `Ctrl+A` 全选、`Ctrl+C` 复制色号矩阵、
`Ctrl+V` 从 Excel 粘贴，以及 `Enter` 使用当前颜色填充选区。
