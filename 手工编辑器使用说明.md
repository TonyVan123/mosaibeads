# MOSAIBeads 手工拼豆编辑器

这是一个独立的小工具，不会覆盖原来的 MOSAIBeads 图片转换程序。软件内置两份 58×57 图纸：

1. 原始识别版
2. 脸型、吊带和右眼高光调整版

## 快速操作

- 在顶部“内置版本”下拉框切换两版图纸。
- 右侧只显示当前图纸已经使用的颜色，并按用量从多到少排列。
- 点击“展开完整色板”会打开类似 Excel 颜色选择器的色块矩阵；每个色块直接显示品牌色号，支持搜索，点击后立即选中并收起。
- “画笔”模式可按住鼠标左键拖动上色。
- “矩形选择”模式可框选很多格；点击行号或列号能选整行/整列，再点击“填充选区”或按 Enter。
- `Ctrl+C`/`Ctrl+V` 可复制粘贴矩形色号，`Ctrl+A` 可全选。
- 在任意格子上单击鼠标右键，可以吸取该格颜色。
- 鼠标滚轮上下移动图纸；按住 Shift 滚动可左右移动。
- 使用“－”“＋”缩放，或点击“适合窗口”。
- `Ctrl+Z` 撤销，`Ctrl+Y` 重做，`Ctrl+S` 保存，`Ctrl+O` 打开项目。

## 保存与导出

- “保存项目”生成 JSON 文件，以后可以继续打开编辑，所有格子和色板都不会丢失。
- “导出图纸”会生成拼豆效果 PNG、纯像素 PNG、带色号图纸 PNG、PDF、材料用量 CSV、JSON 项目文件和可编辑 Excel。
- Excel 里可以整行/整列修改背景色，再用 `MOSAIBeads_Excel_Converter.exe` 转回图纸。
- 导出的红色粗线是 29×29 实体底板边界，深色中线每 5 格出现一次。

## 防误操作

- 切换版本、打开其他项目或关闭软件时，如果当前修改尚未保存，软件会先询问是否保存。
- 每次按住左键拖动算作一次操作，因此一次撤销可以撤销整笔涂画。
- 当前选中的颜色始终用橙色粗框和勾号标出。
